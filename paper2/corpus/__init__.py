"""Paper 2 — linker-accuracy benchmark corpus (Phase 2).

Layer 1 (``primitives``) is the verified foundation: dual-surface
registration, per-pipeline isolation (registries *and* storage), and the
shadow-all-strategies capture that layers 3/4 depend on. Mechanism-class
"difficulty" logic lives in later layers, not here.
"""
